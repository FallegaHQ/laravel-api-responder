# Laravel API Responder Configuration

# API Prefix (set to empty string for no prefix)
API_PREFIX=api

# Cache Driver
API_RESPONDER_CACHE_DRIVER=redis

# Webhook Secret (if using webhooks)
WEBHOOK_SECRET=
